const {cartDetails,setQuantity,saveCart,money,toast,session}=TechStore;
const list=document.getElementById('carrito');const subtotalEl=document.getElementById('subtotal');const shippingEl=document.getElementById('shipping');const totalEl=document.getElementById('total');const label=document.getElementById('cart-items-label');const clearButton=document.getElementById('clear-cart');const checkout=document.getElementById('checkout');
function renderCart(){const items=cartDetails();const units=items.reduce((sum,item)=>sum+item.quantity,0);const subtotal=items.reduce((sum,item)=>sum+item.price*item.quantity,0);const shipping=subtotal>0&&subtotal<1000000?25000:0;label.textContent=`(${units})`;subtotalEl.textContent=money(subtotal);shippingEl.textContent=shipping?money(shipping):'Gratis';totalEl.textContent=money(subtotal+shipping);clearButton.hidden=!items.length;checkout.disabled=!items.length;list.innerHTML=items.length?items.map(item=>`<article class="cart-item"><img src="${item.image}" alt="${item.name}" onerror="this.hidden=true"><div class="cart-item-info"><span class="category">${item.category==='laptops'?'PORTÁTIL':'CELULAR'}</span><h3>${item.name}</h3><span class="item-price">${money(item.price)}</span><button class="remove-button" data-action="remove" data-id="${item.id}">Eliminar</button></div><div class="quantity-control" aria-label="Cantidad de ${item.name}"><button data-action="minus" data-id="${item.id}" aria-label="Disminuir">−</button><output>${item.quantity}</output><button data-action="plus" data-id="${item.id}" aria-label="Aumentar" ${item.quantity>=item.stock?'disabled':''}>+</button></div><strong class="line-total">${money(item.price*item.quantity)}</strong></article>`).join(''):`<div class="empty-cart"><div class="empty-icon">⌁</div><h3>Tu carrito está esperando</h3><p>Explora el catálogo y agrega los equipos que te encanten.</p><a class="primary-button" href="index.html">Ver productos <span>→</span></a></div>`;}
list.addEventListener('click',event=>{const button=event.target.closest('button[data-action]');if(!button)return;const item=cartDetails().find(i=>i.id===button.dataset.id);if(!item)return;if(button.dataset.action==='remove'){setQuantity(item.id,0);toast(`${item.name} se eliminó del carrito`)}else{const next=item.quantity+(button.dataset.action==='plus'?1:-1);setQuantity(item.id,next);if(next>item.stock)toast('No hay más unidades disponibles','error')}renderCart()});
clearButton.addEventListener('click',()=>{if(!cartDetails().length)return;saveCart([]);renderCart();toast('Carrito vaciado')});
checkout.addEventListener('click',()=>{if(!cartDetails().length)return;if(!session()){sessionStorage.setItem('techz_return','carrito.html');toast('Inicia sesión para continuar','error');setTimeout(()=>location.href='login.html',900);return}saveCart([]);renderCart();toast('¡Compra realizada con éxito!');});
renderCart();
