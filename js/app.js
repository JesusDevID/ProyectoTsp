const {PRODUCTS,addToCart,money,toast}=TechStore;
const grid=document.getElementById('productos');const search=document.getElementById('busqueda');const countLabel=document.getElementById('results-count');let category='todos';
const escapeHtml=value=>String(value).replace(/[&<>'"]/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[char]));
function render(){const term=search.value.trim().toLocaleLowerCase('es');const filtered=PRODUCTS.filter(p=>(category==='todos'||p.category===category)&&p.name.toLocaleLowerCase('es').includes(term));countLabel.textContent=`${filtered.length} ${filtered.length===1?'producto':'productos'}`;grid.innerHTML=filtered.length?filtered.map(p=>`<article class="product-card"><div class="product-media">${p.badge?`<span class="product-badge">${escapeHtml(p.badge)}</span>`:''}<img src="${escapeHtml(p.image)}" alt="${escapeHtml(p.name)}" loading="lazy" onerror="this.src='data:image/svg+xml,%3Csvg xmlns=%22http://www.w3.org/2000/svg%22 width=%22600%22 height=%22400%22%3E%3Crect width=%22100%25%22 height=%22100%25%22 fill=%22%23eef2f8%22/%3E%3Ctext x=%2250%25%22 y=%2250%25%22 text-anchor=%22middle%22 fill=%22%236b7280%22 font-size=%2224%22%3ETech Z%3C/text%3E%3C/svg%3E'"></div><div class="product-info"><span class="category">${p.category==='laptops'?'PORTÁTIL':'CELULAR'}</span><h3>${escapeHtml(p.name)}</h3><p class="description">${escapeHtml(p.description)}</p><div class="product-meta"><div><span class="price">${money(p.price)}</span><small>${p.stock<=4?`Solo quedan ${p.stock}`:`${p.stock} disponibles`}</small></div><button class="add-button" data-id="${p.id}" aria-label="Agregar ${escapeHtml(p.name)} al carrito"><svg aria-hidden="true" viewBox="0 0 24 24"><path d="M12 5v14M5 12h14"/></svg></button></div></div></article>`).join(''):`<div class="empty-state"><span>⌕</span><h3>No encontramos resultados</h3><p>Prueba con otra búsqueda o categoría.</p></div>`;}
search.addEventListener('input',render);document.querySelectorAll('.filter-chip').forEach(button=>button.addEventListener('click',()=>{document.querySelector('.filter-chip.active')?.classList.remove('active');button.classList.add('active');category=button.dataset.category;render()}));grid.addEventListener('click',event=>{const button=event.target.closest('.add-button');if(!button)return;const result=addToCart(button.dataset.id);toast(result.message,result.ok?'success':'error');if(result.ok){button.classList.add('added');button.innerHTML='✓';setTimeout(()=>{button.classList.remove('added');button.innerHTML='<svg aria-hidden="true" viewBox="0 0 24 24"><path d="M12 5v14M5 12h14"/></svg>'},900)}});render();
